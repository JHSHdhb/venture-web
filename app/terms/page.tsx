import { PlaceholderPage } from "@/components/shared/PlaceholderPage";
import { placeholderPages } from "@/lib/site-data";

export default function TermsPage() {
  return <PlaceholderPage {...placeholderPages.terms} />;
}
